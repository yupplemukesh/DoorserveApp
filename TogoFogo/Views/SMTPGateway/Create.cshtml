@model TogoFogo.Models.SMTPGatewayModel
@{
    ViewBag.Title = "Create";
    Layout = "~/Views/Shared/_LayoutPage1.cshtml";
}

        <div class="panel-heading">
            <h3 class="m-t-0 m-b-5">New Smtp Gateway</h3>
        </div>


<div class="panel-body">
    @using (Html.BeginForm("Create", "SMTPGateway", FormMethod.Post, new { enctype = "multipart/form-data" }))
    {
        @Html.HiddenFor(x => x.GatewayId)
        <!--/.row-->



        @Html.Partial("_CreateEditSMTPGatewayForm", Model)
        <div class=" row form-group">
            <div class="text-center">
                <button class="btn btn-secondary">
                    Reset
                </button>
                <button class="btn btn-primary">
                    Save
                </button>

            </div>
        </div>


    }
</div>
@section scripts{
    @Scripts.Render("~/bundles/jqueryval")
}
